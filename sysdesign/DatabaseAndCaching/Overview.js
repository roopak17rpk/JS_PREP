// # Browser Caching Overview

// Server --> *API Response* --> **Browser**

// There are different layers within the browser where we can cache the data:

// Server --> API Response ------->
// (within Browser we have 3 layers)[HTTP Cache | Service Worker | API Cache] --> application(state management)

/**
 * tanstack, apollo, react query uses works on API Cache
 */

/**
 * 											Databases
 * local storage, session storage, indexedDB, cookies
 *
 */
